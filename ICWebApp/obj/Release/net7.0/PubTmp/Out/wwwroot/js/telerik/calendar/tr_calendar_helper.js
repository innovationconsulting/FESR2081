function tr_calendar_helper_StyleCalendar() {
    //var cells = $(".k-slot-cell");
    //var currentDate = new Date();
    //var date = new Date(Date.parse($(".k-lg-date-format").text()));

    //var todayPassed = false;
    //var firstDayOfMonthPassed = false;

    //cells.each(function (e) {
    //    cells[e].classList.remove("k-event-passed");
    //})

    //cells.each(function (e)
    //{
    //    if (cells[e].getElementsByTagName("span")[0].innerHTML.replace("<!--!-->", "").replace("<!--!-->", "").trim() == 1) {
    //        firstDayOfMonthPassed = true;
    //    }

    //    if (date.getMonth() + 1 >= currentDate.getMonth() + 1 &&
    //        firstDayOfMonthPassed == true &&
    //        cells[e].getElementsByTagName("span")[0].innerHTML.replace("<!--!-->", "").replace("<!--!-->", "").trim() > currentDate.getDate()) {
    //        todayPassed = true;
    //    }

    //    if (todayPassed == false) {
    //        if (!cells[e].classList.contains("k-event-passed")) {
    //            cells[e].classList.add("k-event-passed");
    //        }
    //    }
    //});
}